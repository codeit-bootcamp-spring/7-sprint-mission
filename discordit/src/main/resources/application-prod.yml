logging:
  level:
    root: info
server:
  port: 80

discodeit:
  storage:
    type: s3

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/discodeit_prod
    username: discodeit_prod
    password: discodeit1234
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
