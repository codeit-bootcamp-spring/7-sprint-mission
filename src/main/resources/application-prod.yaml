server:
  port: 80

spring:
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/discodeit
    username: discodeit_user
    password: discodeit1234
    driver-class-name: org.postgresql.Driver

logging:
  level:
    root: info
    com.sprint.mission.discodeit: info