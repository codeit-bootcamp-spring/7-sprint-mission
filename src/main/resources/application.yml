spring:
  profiles:
    active: dev
  config:
    import: optional:classpath:actuator.yml
  boot:
    admin:
      client:
        instance:
          name: discodeit
logging:
  level:
    root: INFO
    org.springframework.security: trace
